# ROOM Manager — Claude / Cursor 用知識

このリポジトリは、ChatGPT で作られたリフォーム現場の見積・案件管理アプリ **ROOM Manager** です。Claude（Cursor）が続きの改修をするときの前提です。

ChatGPT アカウントそのものには、メールアドレスだけでは入れません。会話履歴の全文は、利用者が ChatGPT からデータエクスポートした ZIP / `conversations.json` をアプリの「AI取込」タブで変換して追加します。

## アプリ概要

- 単一の静的サイト。本番想定は `index.html` を Vercel などに置くだけ。
- ログインはデモ固定: ID `room` / パスワード `1234`。本格ログインは未実装（ヘルプに「次は Firebase」とある）。
- データはブラウザの `localStorage` のみ。サーバー同期はない。

## 保存キー

| キー | 内容 |
|---|---|
| `room_login` | ログイン状態 |
| `room_cases_v1` | 保存済み案件一覧 |
| `room_current_v1` | いま編集中の案件 |
| `room_master_v1` | 単価マスター |
| `room_claude_import_v1` | ChatGPT→Claude 変換結果の要約 |

## 画面

- ダッシュ: 今月売上・粗利、担当別粗利バー、案件一覧
- 案件: 番号・物件名・取引先・担当・状態・面積・売価・粗利手入力・メモ・完工写真
- 見積: 工事条件入力、材料数量概算、原価内訳、粗利率
- 単価: 問屋・施工歩掛・材料単価。データJSON出力あり
- AI取込: ChatGPT エクスポートを Claude 用 Markdown に変換
- 使い方: デモログインと保存先の説明

## 業務ルール（コードに埋め込み）

- 問屋は **渡辺パイプのみ**
- メーカー: LIXIL / TOTO / Panasonic / Takara Standard / AICA（メーカー加算あり）
- 工事ランク: 表層 / 標準フルリノベ / 造作・配管多め
- 担当: 管理者 / 従業員
- 状態: 見積中 / 工事中 / 完工 / 請求済 / 入金済
- 売価は税込。粗利計算の売上は税抜（÷1.1）
- 粗利は手入力があればそれを使い、なければ `税抜売上 - 総原価`
- 粗利率の色: 25%未満赤、30%以上緑、それ以外オレンジ
- 予備費は小計 × 予備費率

## 見積の考え方

数量は面積・ふかし・間仕切りなどから概算。単価マスターの歩掛（パーチ1枚㎡、フローリング1ケース㎡ など）を使う。設備は UB / キッチン / 洗面 / トイレ / 給湯器サイズの固定単価表。

改修時は `calc()` と `mat()` の計算を壊さないこと。サンプル現場は「クレス不動産」。

## ChatGPT データの取り込み方

1. ChatGPT にログイン → 設定 → データコントロール → データをエクスポート
2. 届いた ZIP、または中の `conversations.json` を「AI取込」にアップロード
3. 変換された Markdown をダウンロードする
4. Claude.ai のプロジェクトにファイル追加するか、このリポジトリへコミットする

## 関連ブランチ（未マージの別作業）

- 仲介会社の公開メール一覧
- ダッシュボードのフォルダごとの状況
